---
trigger: always_on
---

When you make commit on github, you should confirm to make the app version up on js/config.js

IF the commit is different from previous commit, you should up the secondary version.